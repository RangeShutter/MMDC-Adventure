| SECTION                                 | GUIDING PROMPTS                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | ANSWER |
| --------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| Context for This Revision               | Briefly describe the state of your design before you made persistence-related updates.<br><br>Consider:<br>Which artifacts existed (CRC cards, method dictionary, diagrams, etc.)<br>How responsibilities were previously organized<br>Where long-term data handling was not yet addressed<br><br>Keep this concise. The goal is to establish a baseline for comparison.                                                                                                                                                         | **Before:** Design artifacts described a **CSV-backed** desktop app: CRC-and-Method-Dictionary.md (CRC + method dictionary), Architecture-Overview.md (layers, component/sequence diagrams). **Updated target artifacts:** [CRC-and-Method-Dictionary-Updated.md](CRC-and-Method-Dictionary-Updated.md), [Architecture-Overview-Updated.md](Architecture-Overview-Updated.md). Persistence lived in `AbstractCsvListRepository` subclasses and `PayrollRepository`; `ApplicationContext` wired CSV concretes. Long-term storage, pay period history, batch runs, and audit were **not** in the domain model or CRC. HM1/HM3 narrative plans existed; HM4 is our **proposed** additive JDBC + HM3 update set—not yet merged into the official CRC file. |
| Persistence-Related Responsibilities    | Based on your Homework 3 planning and current reasoning:<br>Where does persistence-related responsibility live in your design?<br>Which parts of the system are responsible for:<br>\- initiating save or retrieval of data,<br>\- coordinating with stored state,<br>\- maintaining long-term data consistency?<br>Which parts of the system do not own these responsibilities?<br><br>Explain why these responsibilities are placed where they are. Focus on responsibility placement, not tools, APIs, or storage mechanisms. | **Open/close DB connections:** `DatabaseConnectionManager` (proposed) — not UI or domain entities.<br>**SQL load/save, row mapping:** `*JdbcRepository` implementing `I*Repository` — not services (no SQL).<br>**Initiating save/load for use cases:** Services (`EmployeeService`, `PayrollProcessor`, etc.) — not UI.<br>**Business rules, validation, payroll math:** Services + `util` + domain `Validatable` — not repositories.<br>**Batch run, finalize period, append audit:** `PayrollProcessor` (extended) orchestrating repos — not `EmployeeProfile` (no direct DB).<br>**Formatting pay text:** `PayrollReport` — not repositories.<br>**Wiring implementations:** `ApplicationContext`.<br><br>**Why:** Matches our existing pattern—repositories “load/save only”; services already delegate persistence and own in-memory workflow. HM3 audit runs **after** successful repository save/finalize, triggered from the service layer, not UI. UI continues to call `ctx.getPayrollProcessor()` etc. |
| Artifact Updates (What Changed and Why) | For each design artifact you updated, describe:<br><br>Artifact Name / Type (e.g., CRC card for PayrollProcessor, Method Dictionary entry for generatePayslip, Class Diagram)<br>\- What changed in this artifact?<br>\- What responsibility became clearer or more explicit?<br>\- How does this artifact now make persistence visible in the design?<br><br>Repeat this subsection as needed for multiple artifacts. You do not need to include the artifact itself here—only explain the changes.                             | *Note: “Changed” = **proposed in HM4**; official CRC/diagrams still show CSV until group merge.*<br><br>**1. CRC cards — Group A (HM1 / MySQL JDBC)**<br>- **Added:** `DatabaseConnectionManager`, `AbstractJdbcRepository`, `EmployeeJdbcRepository`, `AttendanceJdbcRepository`, `LeaveRequestJdbcRepository`, `UserCredentialJdbcRepository`, `ItTicketJdbcRepository`, `PayrollJdbcRepository`.<br>- **Updated:** `ApplicationContext` — wire JDBC repos for MySQL.<br>- **Unchanged (legacy):** CSV repos remain documented; not removed.<br>- **Clarity:** Persistence visible as JDBC layer parallel to CSV; swap at composition root.<br><br>**2. CRC cards — Group B (HM3)**<br>- **Added domain:** `PayPeriod`, `PayrollRunSnapshot`, `AuditLogEntry`.<br>- **Added repos:** `IPayPeriodRepository`, `PayPeriodJdbcRepository`, `IPayrollRunRepository`, `PayrollRunJdbcRepository`, `IAuditLogRepository`, `AuditLogJdbcRepository`.<br>- **Clarity:** Period workflow and frozen history are first-class persisted concepts.<br><br>**3. CRC cards — Group C (services)**<br>- **Updated:** `PayrollProcessor` — batch run, load snapshots, finalize, audit append.<br>- **Updated:** `PayrollReport` — register and deduction summary from snapshots.<br>- **Explicit:** UI modules — no new persistence responsibilities.<br><br>**4. Method dictionary (proposed entries)**<br>- New methods on `PayrollProcessor` (e.g. `runPayrollForPeriod`, `finalizePeriod`, finalized-branch `getPayrollForEmployee`); on repos: `saveAllForPeriod`, `findByPeriod`, `append`.<br>- **Clarity:** Persistence operations named at service/repo boundary, not in UI handlers.<br><br>**5. Class diagram (Architecture-Overview §7 extension)**<br>- Add domain + JDBC repo classes; show `ApplicationContext` → JDBC implementations; `PayrollProcessor` → new repo interfaces.<br>- **Clarity:** HM1 storage swap + HM3 entities on one diagram.<br><br>**6. Sequence diagrams**<br>- **Update:** Employee update, Payroll view — `*JdbcRepository` + `Database` instead of CSV.<br>- **New:** Batch payroll run + finalize; audit on pay setting save.<br>- **Clarity:** Finalized period reads snapshots; draft uses live compute + attendance. |
| Design Consistency Across Artifacts     | Reflect on your updated artifacts as a whole:<br>\- Do all artifacts tell the same design story?<br>\- Are persistence responsibilities consistently placed across artifacts?<br>\- Where do responsibilities feel well-defined?<br>\- Where are there still ambiguities or tensions?<br><br>If inconsistencies exist, note them—do not resolve them yet.                                                                                                                                                                        | **Aligned story:** Layered app, repository interfaces, services orchestrate, MySQL via JDBC, HM3 concepts on top—consistent across HM4, Architecture-Overview extension intent, and payroll expansion plan.<br><br>**Gaps today:** Official CRC-and-Method-Dictionary.md still lists only CSV repos; no `PayPeriod` / audit types; sequence diagrams in Architecture-Overview still show `employees.csv`. Code matches CSV baseline.<br><br>**Well-defined:** Repository-only I/O; `PayrollProcessor` as payroll use-case hub; role boundaries unchanged.<br><br>**Ambiguities:** Whether `AuthenticationService` stays CSV-first or moves credentials to MySQL in Phase 1; naming of period IDs; single vs dual screens for batch run (future payroll module vs `EmployeeProfile`). |
| Provisional Decisions and Trade-offs    | Document the tentative nature of your current design:<br>\- Which persistence-related decisions feel most provisional?<br>\- What trade-offs did you recognize in your responsibility placement?<br>\- What assumptions are you making that may need to be revisited?<br><br>This section is especially important for mentor feedback.                                                                                                                                                                                           | **Most provisional:** MySQL connection config; full schema; CSV migration script; transaction scope; finalized-period edit policy; keeping CSV fallback flag in `ApplicationContext`.<br><br>**Trade-offs:**<br>- **Pro:** JDBC + existing `I*Repository` = smallest change to services/UI.<br>- **Con:** Services may retain “load all, mutate, save all” from CSV era—acceptable for coursework, may need refinement later.<br>- **Pro:** Separate audit and run-snapshot tables—clean HM3 reporting.<br>- **Con:** More classes than ORM for same tables.<br><br>**Assumptions to revisit:** Monthly pay rhythm; Philippine statutory rules unchanged; ~38 employees; HR never edits pay; action-type audit sufficient for v1. |
| Open Questions for Group Discussion     | List:<br>questions you want to raise with your group,<br>areas where you expect disagreement or alternative approaches,<br>aspects of the design you are unsure how to refine.<br><br>These notes will guide Week 3 group alignment.                                                                                                                                                                                                                                                                                             | - Confirm **MySQL** install/deployment (local server vs shared instance) and credentials handling (not in repo).<br>- CSV seed **import** strategy and order (employees before payroll rows).<br>- Merge HM4 into official CRC vs maintain separate proposal doc.<br>- Who implements `DatabaseConnectionManager` vs individual JDBC repos (parallel work split).<br>- Finalized period: immutable snapshots only, or also lock live `PayrollData` edits?<br>- New UI module for payroll office vs extending `EmployeeProfile` tabs.<br>- Export deliverable for HM3 reports (print dialog vs file export). |
