# MO-IT113 - Advanced Object-Oriented Programming

## Group Implementation Progress Log - Week 8

Instructions:

- Starting Week 5, your group will maintain one shared Implementation Progress Log throughout Milestone 2.
- Each group member must update their own section, so everyone has visibility into completed work, challenges encountered, and where approaches may differ.
- This task is completed individually. No group convergence summary is required for reporting.

## Individual Progress Updates

### Colin Bactong - Team Leader


| SECTION                                    | GUIDING PROMPTS                                                                                | ANSWER                                                                                                                                                                                                                                                                                                                                          |
| ------------------------------------------ | ---------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Reporting Scope Implemented                | What reports did you enable? Where are reports triggered from?                                 | I coordinated the reporting work and verified that the existing employee payslip flow and the new Payroll Summary flow are both triggered through `EmployeeProfile`. The payslip remains available from the employee salary computation view, while the all-employee summary is available through the Payroll Summary tab for management users.<br><br><b>Update</b><br>Following mentor feedback, the Payroll Summary is now department-specific (HR, Payroll, IT/Admin, Normal Employee) rather than an all-employee listing. JasperReports PDF export was added alongside the existing payslip and text-summary flows, both triggered from the Payroll Summary tab in `EmployeeProfile`. |
| Data Source Verification                   | Where does report data come from?                                                              | I confirmed that reports use the same service layer that now receives data through JDBC repositories when `storage.mode=jdbc`. Employee, attendance, and payroll setting data are loaded through repositories, not directly from GUI code or manually read CSV files.<br><br><b>Update</b><br>Persistence was fully consolidated to JDBC-only: `ApplicationContext` wires only `*JdbcRepository` implementations and there is no `storage.mode` switch or CSV fallback. Report data flows through repository interfaces into services and then to MySQL database `gear.hr`. |
| Logic Reuse Confirmation                   | Which existing payroll logic is reused for reports?                                            | The reports reuse `PayrollProcessor.processPayroll`, which already combines `PayrollData`, employee information, and attendance hours. Formatting is handled by `PayrollReport`, so SQL and UI handlers do not duplicate payroll calculations.<br><br><b>Update</b><br>`collectDepartmentResults()` was added so department filtering happens before `PayrollReport.formatSummary` and the JasperReports PDF export, ensuring both outputs reuse the same filtered `PayrollResult` list from `PayrollProcessor`. |
| Resolutions or Current Status              | Do your report classes contain only final values and no persistence/UI logic?                  | Current status is acceptable: `PayrollResult` holds computed final values, `PayrollReport` formats those values, and persistence remains inside repositories. Any UI code only triggers generation and saving, not payroll formulas.<br><br><b>Update</b><br>Repository interfaces and JDBC implementation classes were updated to document MySQL as the sole storage layer, and stale CSV references in comments were removed. The separation between persistence, computation, and formatting remains intact. |
| Authentication Context (Confirmation Only) | Reports are generated for logged-in employee or HR users. No authentication code was modified. | Confirmed. No authentication logic was modified. Access depends on the existing role grouping, where HR, Payroll/Accounting, and IT/Admin users can access management reporting, while normal employees only see their own payroll view.<br><br><b>Update</b><br>No authentication changes were made during mentor-feedback remediation. Reporting access still depends on `RoleGroup` classification from `user_credentials`. |
| Issues Revealed by Reporting               | What design issues did reporting expose?                                                       | Reporting exposed the need for a clear boundary between data loading, payroll computation, and output formatting. This was handled by keeping repository loading, `PayrollProcessor`, and `PayrollReport` separate.<br><br><b>Update</b><br>Mentor feedback revealed that maintaining dual CSV/JDBC persistence and generating all-employee summaries were misleading relative to requirements. Both were addressed by consolidating to JDBC-only storage and scoping summaries to the selected department. |
| Ready for Testing?                         | Reports generate correct values and are ready for testing.                                     | Ready for testing. The next step is to verify sample SQL data, generate monthly summaries, and compare totals against individual payroll results.<br><br><b>Update</b><br>Verification can use default accounts from `sql/seed.sql` (documented in README). Generate department summaries for a seeded month and compare department totals against the sum of individual payslip results for employees in that department. |


### Charlize Bactong


| SECTION                                    | GUIDING PROMPTS                                                                                | ANSWER                                                                                                                                                                                                                                                              |
| ------------------------------------------ | ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Reporting Scope Implemented                | What reports did you enable? Where are reports triggered from?                                 | I focused on the attendance and employee data side used by reports. The payroll summary depends on all employees and their worked hours for the selected month, so I verified that the existing GUI flow can request the month and pass it into payroll processing.<br><br><b>Update</b><br>The payroll summary now filters employees by `RoleGroup` using each employee's login role from `user_credentials` before computing results. Attendance hours from `AttendanceService` still drive per-employee payroll within the selected department only. |
| Data Source Verification                   | Where does report data come from?                                                              | Report data comes from the configured repositories. In JDBC mode, employee rows come from the `employees` table and attendance rows come from `attendance_records`, then services expose them to payroll processing.<br><br><b>Update</b><br>Confirmed that `EmployeeJdbcRepository` and `AttendanceJdbcRepository` supply report data from MySQL. Sample data and default login accounts in `sql/seed.sql` make setup and verification significantly easier for reviewers. |
| Logic Reuse Confirmation                   | Which existing payroll logic is reused for reports?                                            | I confirmed that worked hours are still calculated by `AttendanceService.getWorkedHoursForMonth`, and payroll summaries do not calculate attendance duration inside the report formatter or UI button handler.<br><br><b>Update</b><br>Department filtering in `collectDepartmentResults()` does not duplicate attendance math; it only selects which employees are passed to `PayrollProcessor`. `AttendanceService.getWorkedHoursForMonth` remains the single source for worked hours. |
| Resolutions or Current Status              | Do your report classes contain only final values and no persistence/UI logic?                  | The current reporting flow keeps attendance aggregation in the service layer. The report output only receives computed `PayrollResult` values, so attendance persistence does not leak into `PayrollReport`.<br><br><b>Update</b><br>Fixed-state values were migrated from loose strings to type-safe enums: `EmploymentStatus`, `LeaveStatus`, `AttendanceStatus`, and `TicketStatus`. Models parse database strings via `fromString()` at the boundary, improving type safety and reducing invalid status values. |
| Authentication Context (Confirmation Only) | Reports are generated for logged-in employee or HR users. No authentication code was modified. | Confirmed. No authentication code was changed for attendance-based reporting. The same management screen access determines who can generate payroll summaries.<br><br><b>Update</b><br>No changes were made to authentication during this week's remediation work. |
| Issues Revealed by Reporting               | What design issues did reporting expose?                                                       | The report feature highlighted that attendance records must be consistently formatted in SQL, especially date and time fields. This will be covered in testing by checking month filtering and worked-hour totals.<br><br><b>Update</b><br>Enum migration prevents misspelled or invalid status strings from entering the system. SQL attendance rows must remain consistently formatted so month filtering and worked-hour totals stay accurate for department reports. |
| Ready for Testing?                         | Reports generate correct values and are ready for testing.                                     | Ready for testing with sample June attendance rows from the SQL database and matching payroll settings.<br><br><b>Update</b><br>`StatusEnumTest` verifies enum parsing for all four status types. June attendance rows from `sql/seed.sql` can be used to confirm worked-hour totals match expected department payroll results. |


### Angelica Mae Calipayan


| SECTION                                    | GUIDING PROMPTS                                                                                | ANSWER                                                                                                                                                                                                                                                          |
| ------------------------------------------ | ---------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Reporting Scope Implemented                | What reports did you enable? Where are reports triggered from?                                 | I focused on payroll reporting output. The existing employee payslip uses `PayrollReport.format`, and the new Payroll Summary uses `PayrollReport.formatSummary` to show all employees for a selected month and support saving the result as a `.txt` document.<br><br><b>Update</b><br>`PayrollReport.formatSummary` now receives pre-filtered department results from `collectDepartmentResults()`. **Export PDF (JasperReports)** was added via `JasperPayrollReportService` and `src/reports/payroll_summary.jrxml`, alongside the existing `.txt` save option. |
| Data Source Verification                   | Where does report data come from?                                                              | The report data comes from the JDBC-backed service flow when JDBC mode is active: payroll settings from `payroll_settings`, employees from `employees`, and attendance from `attendance_records`. CSV remains only as fallback if `storage.mode=csv`.<br><br><b>Update</b><br>Payroll settings are loaded exclusively through `PayrollJdbcRepository` from the `payroll_settings` table. There is no CSV fallback; JDBC is the only persistence mechanism. |
| Logic Reuse Confirmation                   | Which existing payroll logic is reused for reports?                                            | Payroll reports reuse `PayrollProcessor.processPayroll` and `PayrollUtils` for deductions. I avoided recalculating SSS, PhilHealth, Pag-IBIG, withholding tax, gross pay, or net pay inside the report formatter.<br><br><b>Update</b><br>`PayrollProcessor` and `PayrollUtils` remain the sole calculation path. JasperReports fills from the same `PayrollResult` list used by the on-screen text summary, so PDF and text outputs share identical computed values. |
| Resolutions or Current Status              | Do your report classes contain only final values and no persistence/UI logic?                  | Current status is clean: `PayrollReport` only formats computed results and totals, while `PayrollProcessor` performs the calculations. The formatter has no SQL, repository, or Swing dependencies.<br><br><b>Update</b><br>`PayrollReport` and `JasperPayrollReportService` contain formatting and export logic only—no SQL, repository access, or payroll formulas. PDF generation is document export, not database persistence. |
| Authentication Context (Confirmation Only) | Reports are generated for logged-in employee or HR users. No authentication code was modified. | Confirmed. The report feature uses existing access rules. Normal employees remain limited to personal payroll, while HR, Payroll/Accounting, and IT/Admin users can generate the broader summary.<br><br><b>Update</b><br>HR, Payroll/Accounting, and IT/Admin users can generate department-specific summaries. Normal employees continue to see only their personal payslip view. |
| Issues Revealed by Reporting               | What design issues did reporting expose?                                                       | Reporting revealed that summary totals should be generated from the same per-employee results used by individual payroll. This prevents different totals between individual payslips and the summary report.<br><br><b>Update</b><br>Mentor feedback noted the summary listed all employees instead of filtering by department. This was fixed with `departmentToRoleGroup()` and `collectDepartmentResults()` so output reflects only the selected department. |
| Ready for Testing?                         | Reports generate correct values and are ready for testing.                                     | Ready for testing. The next check is to compare summary grand totals against the sum of individual generated payroll results.<br><br><b>Update</b><br>`PayrollReportTest` verifies department headers, employee counts, and aggregated totals. `PayrollProcessorTest` covers payroll computation math with in-memory fake repositories. |


### Chelsie Mae Ricafrante


| SECTION                                    | GUIDING PROMPTS                                                                                | ANSWER                                                                                                                                                                                                                                                           |
| ------------------------------------------ | ---------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Reporting Scope Implemented                | What reports did you enable? Where are reports triggered from?                                 | I reviewed the final reporting scope and documentation: individual salary computation/payslip remains in the employee detail flow, and the new Payroll Summary tab generates all-employee reports from Employee Profile Management.<br><br><b>Update</b><br>README now documents the department-specific Payroll Summary workflow: select department and month, Generate Summary, then Save to `.txt` or Export PDF (JasperReports) from the Payroll Summary tab. |
| Data Source Verification                   | Where does report data come from?                                                              | I confirmed in the documentation and test planning that report data is SQL-backed in JDBC mode and flows through repositories and services. The report feature should not read CSV directly unless the configured backend is intentionally switched to CSV mode.<br><br><b>Update</b><br>README and `database.properties` document JDBC-only setup. `sql/schema.sql` and `sql/seed.sql` provide initialization scripts and sample data. A default test accounts table in README supports login verification per role group. |
| Logic Reuse Confirmation                   | Which existing payroll logic is reused for reports?                                            | I confirmed that the report feature reuses `PayrollProcessor`, `PayrollResult`, and `PayrollReport`, instead of introducing separate payroll formulas in the UI or database layer.<br><br><b>Update</b><br>Documentation confirms the services/repositories pattern is unchanged: reports consume service output and do not introduce separate payroll logic in the UI or database layer. |
| Resolutions or Current Status              | Do your report classes contain only final values and no persistence/UI logic?                  | The status is ready: report formatting is separate from persistence, and saving to `.txt` is treated as document export rather than database persistence. Documentation now reflects JDBC as the main source and CSV as fallback.<br><br><b>Update</b><br>README was updated to describe JDBC as the only persistence layer. Both `.txt` and JasperReports PDF exports are documented as document export, not database writes. |
| Authentication Context (Confirmation Only) | Reports are generated for logged-in employee or HR users. No authentication code was modified. | Confirmed. Authentication code was not modified. Role access is still controlled by `RoleGroup`, including Payroll access for Accounting roles and admin access for IT.<br><br><b>Update</b><br>`RoleGroup` mapping for Accounting roles (Accounting Head, Account Manager, etc.) remains documented in README. No authentication code was modified during remediation. |
| Issues Revealed by Reporting               | What design issues did reporting expose?                                                       | Reporting made it clear that the README, unit test planning, and implementation logs needed to mention JDBC repository usage and SQL connection coverage. This was addressed in the documentation updates.<br><br><b>Update</b><br>A JUnit 4 test suite was added under `test/` (5 classes, 20 tests) covering payroll math, role mapping, status enums, and department report formatting. README testing section was expanded so reviewers can find and run tests without a database. |
| Ready for Testing?                         | Reports generate correct values and are ready for testing.                                     | Ready for testing and Milestone 2 draft review. The remaining work is to run sample report generation with SQL data and confirm exported `.txt` output.<br><br><b>Update</b><br>All 20 JUnit tests compile and pass without a database. README includes compile and run instructions using `lib/junit-4.13.2.jar` and `lib/hamcrest-core-1.3.jar`. |


