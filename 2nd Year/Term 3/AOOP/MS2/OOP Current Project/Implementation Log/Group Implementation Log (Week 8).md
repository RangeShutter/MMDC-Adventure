# MO-IT113 - Advanced Object-Oriented Programming

## Group Implementation Progress Log - Week 8

Instructions:

- Starting Week 5, your group will maintain one shared Implementation Progress Log throughout Milestone 2.
- Each group member must update their own section, so everyone has visibility into completed work, challenges encountered, and where approaches may differ.
- This task is completed individually. No group convergence summary is required for reporting.

## Individual Progress Updates

### Colin Bactong - Team Leader


| SECTION                                    | GUIDING PROMPTS                                                                                | ANSWER                                                                                                                                                                                                                                                                                                                                          |
| ------------------------------------------ | ---------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Reporting Scope Implemented                | What reports did you enable? Where are reports triggered from?                                 | I coordinated the reporting work and verified that the existing employee payslip flow and the new Payroll Summary flow are both triggered through `EmployeeProfile`. The payslip remains available from the employee salary computation view, while the all-employee summary is available through the Payroll Summary tab for management users. |
| Data Source Verification                   | Where does report data come from?                                                              | I confirmed that reports use the same service layer that now receives data through JDBC repositories when `storage.mode=jdbc`. Employee, attendance, and payroll setting data are loaded through repositories, not directly from GUI code or manually read CSV files.                                                                           |
| Logic Reuse Confirmation                   | Which existing payroll logic is reused for reports?                                            | The reports reuse `PayrollProcessor.processPayroll`, which already combines `PayrollData`, employee information, and attendance hours. Formatting is handled by `PayrollReport`, so SQL and UI handlers do not duplicate payroll calculations.                                                                                                  |
| Resolutions or Current Status              | Do your report classes contain only final values and no persistence/UI logic?                  | Current status is acceptable: `PayrollResult` holds computed final values, `PayrollReport` formats those values, and persistence remains inside repositories. Any UI code only triggers generation and saving, not payroll formulas.                                                                                                            |
| Authentication Context (Confirmation Only) | Reports are generated for logged-in employee or HR users. No authentication code was modified. | Confirmed. No authentication logic was modified. Access depends on the existing role grouping, where HR, Payroll/Accounting, and IT/Admin users can access management reporting, while normal employees only see their own payroll view.                                                                                                        |
| Issues Revealed by Reporting               | What design issues did reporting expose?                                                       | Reporting exposed the need for a clear boundary between data loading, payroll computation, and output formatting. This was handled by keeping repository loading, `PayrollProcessor`, and `PayrollReport` separate.                                                                                                                             |
| Ready for Testing?                         | Reports generate correct values and are ready for testing.                                     | Ready for testing. The next step is to verify sample SQL data, generate monthly summaries, and compare totals against individual payroll results.                                                                                                                                                                                               |


### Charlize Bactong


| SECTION                                    | GUIDING PROMPTS                                                                                | ANSWER                                                                                                                                                                                                                                                              |
| ------------------------------------------ | ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Reporting Scope Implemented                | What reports did you enable? Where are reports triggered from?                                 | I focused on the attendance and employee data side used by reports. The payroll summary depends on all employees and their worked hours for the selected month, so I verified that the existing GUI flow can request the month and pass it into payroll processing. |
| Data Source Verification                   | Where does report data come from?                                                              | Report data comes from the configured repositories. In JDBC mode, employee rows come from the `employees` table and attendance rows come from `attendance_records`, then services expose them to payroll processing.                                                |
| Logic Reuse Confirmation                   | Which existing payroll logic is reused for reports?                                            | I confirmed that worked hours are still calculated by `AttendanceService.getWorkedHoursForMonth`, and payroll summaries do not calculate attendance duration inside the report formatter or UI button handler.                                                      |
| Resolutions or Current Status              | Do your report classes contain only final values and no persistence/UI logic?                  | The current reporting flow keeps attendance aggregation in the service layer. The report output only receives computed `PayrollResult` values, so attendance persistence does not leak into `PayrollReport`.                                                        |
| Authentication Context (Confirmation Only) | Reports are generated for logged-in employee or HR users. No authentication code was modified. | Confirmed. No authentication code was changed for attendance-based reporting. The same management screen access determines who can generate payroll summaries.                                                                                                      |
| Issues Revealed by Reporting               | What design issues did reporting expose?                                                       | The report feature highlighted that attendance records must be consistently formatted in SQL, especially date and time fields. This will be covered in testing by checking month filtering and worked-hour totals.                                                  |
| Ready for Testing?                         | Reports generate correct values and are ready for testing.                                     | Ready for testing with sample June attendance rows from the SQL database and matching payroll settings.                                                                                                                                                             |


### Angelica Mae Calipayan


| SECTION                                    | GUIDING PROMPTS                                                                                | ANSWER                                                                                                                                                                                                                                                          |
| ------------------------------------------ | ---------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Reporting Scope Implemented                | What reports did you enable? Where are reports triggered from?                                 | I focused on payroll reporting output. The existing employee payslip uses `PayrollReport.format`, and the new Payroll Summary uses `PayrollReport.formatSummary` to show all employees for a selected month and support saving the result as a `.txt` document. |
| Data Source Verification                   | Where does report data come from?                                                              | The report data comes from the JDBC-backed service flow when JDBC mode is active: payroll settings from `payroll_settings`, employees from `employees`, and attendance from `attendance_records`. CSV remains only as fallback if `storage.mode=csv`.           |
| Logic Reuse Confirmation                   | Which existing payroll logic is reused for reports?                                            | Payroll reports reuse `PayrollProcessor.processPayroll` and `PayrollUtils` for deductions. I avoided recalculating SSS, PhilHealth, Pag-IBIG, withholding tax, gross pay, or net pay inside the report formatter.                                               |
| Resolutions or Current Status              | Do your report classes contain only final values and no persistence/UI logic?                  | Current status is clean: `PayrollReport` only formats computed results and totals, while `PayrollProcessor` performs the calculations. The formatter has no SQL, repository, or Swing dependencies.                                                             |
| Authentication Context (Confirmation Only) | Reports are generated for logged-in employee or HR users. No authentication code was modified. | Confirmed. The report feature uses existing access rules. Normal employees remain limited to personal payroll, while HR, Payroll/Accounting, and IT/Admin users can generate the broader summary.                                                               |
| Issues Revealed by Reporting               | What design issues did reporting expose?                                                       | Reporting revealed that summary totals should be generated from the same per-employee results used by individual payroll. This prevents different totals between individual payslips and the summary report.                                                    |
| Ready for Testing?                         | Reports generate correct values and are ready for testing.                                     | Ready for testing. The next check is to compare summary grand totals against the sum of individual generated payroll results.                                                                                                                                   |


### Chelsie Mae Ricafrante


| SECTION                                    | GUIDING PROMPTS                                                                                | ANSWER                                                                                                                                                                                                                                                           |
| ------------------------------------------ | ---------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Reporting Scope Implemented                | What reports did you enable? Where are reports triggered from?                                 | I reviewed the final reporting scope and documentation: individual salary computation/payslip remains in the employee detail flow, and the new Payroll Summary tab generates all-employee reports from Employee Profile Management.                              |
| Data Source Verification                   | Where does report data come from?                                                              | I confirmed in the documentation and test planning that report data is SQL-backed in JDBC mode and flows through repositories and services. The report feature should not read CSV directly unless the configured backend is intentionally switched to CSV mode. |
| Logic Reuse Confirmation                   | Which existing payroll logic is reused for reports?                                            | I confirmed that the report feature reuses `PayrollProcessor`, `PayrollResult`, and `PayrollReport`, instead of introducing separate payroll formulas in the UI or database layer.                                                                               |
| Resolutions or Current Status              | Do your report classes contain only final values and no persistence/UI logic?                  | The status is ready: report formatting is separate from persistence, and saving to `.txt` is treated as document export rather than database persistence. Documentation now reflects JDBC as the main source and CSV as fallback.                                |
| Authentication Context (Confirmation Only) | Reports are generated for logged-in employee or HR users. No authentication code was modified. | Confirmed. Authentication code was not modified. Role access is still controlled by `RoleGroup`, including Payroll access for Accounting roles and admin access for IT.                                                                                          |
| Issues Revealed by Reporting               | What design issues did reporting expose?                                                       | Reporting made it clear that the README, unit test planning, and implementation logs needed to mention JDBC repository usage and SQL connection coverage. This was addressed in the documentation updates.                                                       |
| Ready for Testing?                         | Reports generate correct values and are ready for testing.                                     | Ready for testing and Milestone 2 draft review. The remaining work is to run sample report generation with SQL data and confirm exported `.txt` output.                                                                                                          |
