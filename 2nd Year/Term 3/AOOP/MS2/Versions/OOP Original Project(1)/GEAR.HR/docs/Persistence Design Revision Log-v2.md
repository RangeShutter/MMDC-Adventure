| SECTION                                 | GUIDING PROMPTS                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | ANSWER |
| --------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| Context for This Revision               | Briefly describe the state of your design before you made persistence-related updates.<br><br>Consider:<br>Which artifacts existed (CRC cards, method dictionary, diagrams, etc.)<br>How responsibilities were previously organized<br>Where long-term data handling was not yet addressed<br><br>Keep this concise. The goal is to establish a baseline for comparison.                                                                                                                                                         | **Design discussion — baseline:** Artifacts described a **CSV-backed** desktop app: [CRC-and-Method-Dictionary.md](CRC-and-Method-Dictionary.md), [Architecture-Overview.md](Architecture-Overview.md). Persistence lived in `AbstractCsvListRepository` and `PayrollRepository`; `ApplicationContext` wired CSV concretes. Pay period history, batch runs, audit, and JDBC were **not** in domain model, CRC, or UML diagrams.<br><br>**v2 target artifacts:** [CRC-and-Method-Dictionary-Updated-v2.md](CRC-and-Method-Dictionary-Updated-v2.md) (CRC with Collaborators column), [Architecture-Overview-Updated-v2.md](Architecture-Overview-Updated-v2.md) (UML class + use case diagrams, JDBC vs ORM §6a). Prior target: `-Updated.md` files. Code in `src` remains CSV-only until implementation. |
| Persistence-Related Responsibilities    | Based on your Homework 3 planning and current reasoning:<br>Where does persistence-related responsibility live in your design?<br>Which parts of the system are responsible for:<br>\- initiating save or retrieval of data,<br>\- coordinating with stored state,<br>\- maintaining long-term data consistency?<br>Which parts of the system do not own these responsibilities?<br><br>Explain why these responsibilities are placed where they are. Focus on responsibility placement, not tools, APIs, or storage mechanisms. | **Design discussion — responsibility matrix:**<br><br>• **Open/close connections** — Owner: `DatabaseConnectionManager`. Does NOT own: UI, domain entities, services.<br>• **SQL load/save, row mapping** — Owner: `*JdbcRepository` implementing `I*Repository`. Does NOT own: services (no SQL in service layer).<br>• **Initiate save/load for use cases** — Owner: services (`EmployeeService`, `PayrollProcessor`, etc.). Does NOT own: UI screens.<br>• **Business rules, validation, payroll math** — Owner: services + `util` + domain `Validatable`. Does NOT own: repositories.<br>• **Batch run, finalize period, audit append** — Owner: `PayrollProcessor` orchestrating repos. Does NOT own: `EmployeeProfile` (no direct DB); repositories alone (no business workflow).<br>• **Format pay text** — Owner: `PayrollReport`. Does NOT own: repositories.<br>• **Wire implementations** — Owner: `ApplicationContext`. Does NOT own: feature layers (they receive wired dependencies only).<br><br>**Delegation rationale:** Matches our CSV-era habit—repositories “load/save only.” Services decide *when* to persist; JDBC repos decide *how* (SQL). Audit runs **after** successful save/finalize in `PayrollProcessor`, not from UI clicks. This placement is why JDBC fits better than ORM: we extend repository classes without merging persistence into domain entities. |
| Artifact Updates (What Changed and Why) | For each design artifact you updated, describe:<br><br>Artifact Name / Type (e.g., CRC card for PayrollProcessor, Method Dictionary entry for generatePayslip, Class Diagram)<br>\- What changed in this artifact?<br>\- What responsibility became clearer or more explicit?<br>\- How does this artifact now make persistence visible in the design?<br><br>Repeat this subsection as needed for multiple artifacts. You do not need to include the artifact itself here—only explain the changes.                             | **Design discussion — v2 artifact updates:**<br><br>**1. CRC cards (v2)** — Added **Collaborators** column on all cards; interaction highlights for delegation chains. Persistence visible as JDBC repo collaborators; UI cards list services only.<br><br>**2. Architecture overview (v2)** — **UML use case diagram (§7a)** links actors to HM3 flows. **Three UML class diagrams (§7b–7d):** domain, persistence (JDBC + legacy CSV), application/presentation. **§6a JDBC vs ORM** comparison table and conclusion.<br><br>**3. Method dictionary** — Planned JDBC/HM3 methods unchanged from `-Updated`; names persistence at service/repo boundary.<br><br>**4. Sequence diagrams** — JDBC + Database participants; finalized vs draft payroll branch; batch finalize + audit flows.<br><br>**5. Planning / revision log (v2)** — ANSWER columns expanded with design discussion, trade-offs, contextual open questions. |
| Design Consistency Across Artifacts     | Reflect on your updated artifacts as a whole:<br>\- Do all artifacts tell the same design story?<br>\- Are persistence responsibilities consistently placed across artifacts?<br>\- Where do responsibilities feel well-defined?<br>\- Where are there still ambiguities or tensions?<br><br>If inconsistencies exist, note them—do not resolve them yet.                                                                                                                                                                        | **Design discussion:**<br><br>**Aligned story:** Layered app; MySQL via JDBC; HM3 periods/snapshots/audit; JDBC chosen over ORM for repository swap and visible finalize transactions—consistent across Architecture v2, CRC v2 collaborators, Planning v2, and HM4 intent.<br><br>**Well-defined:** Repository-only I/O; `PayrollProcessor` as payroll hub; role boundaries; use case ↔ sequence diagram mapping.<br><br>**Gaps vs code:** `src` still CSV; official non-v2 docs unchanged.<br><br>**Ambiguities:** Credential load path in Phase 1; period ID naming; payroll office UI placement; transaction scope (see §16 Architecture v2). |
| Provisional Decisions and Trade-offs    | Document the tentative nature of your current design:<br>\- Which persistence-related decisions feel most provisional?<br>\- What trade-offs did you recognize in your responsibility placement?<br>\- What assumptions are you making that may need to be revisited?<br><br>This section is especially important for mentor feedback.                                                                                                                                                                                           | **Design discussion — JDBC vs ORM trade-offs:**<br><br>**JDBC (chosen) — advantages:** Preserves existing `I*Repository` contracts; explicit finalize/batch transactions; persistence visible in CRC cards and UML diagrams; aligns with course JDBC path; avoids annotating domain entities for mapping.<br><br>**JDBC (chosen) — drawbacks:** More SQL and repository classes to maintain; services may keep CSV-era “load all, mutate, save all” until refactored.<br><br>**ORM (not chosen) — advantages:** Less boilerplate for simple CRUD tables; faster initial entity mapping.<br><br>**ORM (not chosen) — drawbacks:** Hidden query behavior; poor fit for `PayrollData` as map-keyed value object; append-only audit and bulk snapshot inserts need careful mapping rules; would blur domain/persistence boundaries we keep separate in CRC v2.<br><br>**Most provisional decisions:** MySQL connection config; full schema DDL; CSV import script; transaction scope on finalize; finalized-period edit policy; CSV fallback flag in `ApplicationContext`.<br><br>**Design impacts:** `PayrollProcessor` gains orchestration across five repo interfaces plus audit; repositories stay thin. ORM would have shifted mapping responsibility toward domain—migration cost we avoided at this milestone.<br><br>**Assumptions to revisit:** monthly pay rhythm; ~38 employees; HR read-only pay; action-type audit sufficient for v1; JDBC-only for current coursework scope. |
| Open Questions for Group Discussion     | List:<br>questions you want to raise with your group,<br>areas where you expect disagreement or alternative approaches,<br>aspects of the design you are unsure how to refine.<br><br>These notes will guide Week 3 group alignment.                                                                                                                                                                                                                                                                                             | **Design discussion — open questions with context:**<br><br>1. **MySQL deployment** — *Why now:* blocks all JDBC repos. *Options:* local vs shared server; credentials outside repo.<br>2. **CSV import order** — *Why:* employees before payroll rows. *Options:* script vs manual.<br>3. **Merge v2 into official docs** — *Why:* single source of truth for group. *Options:* replace `-Updated` vs keep v2 parallel.<br>4. **Work split** — *Why:* parallel implementation. *Options:* one owner for `DatabaseConnectionManager` vs per-repo JDBC.<br>5. **Finalized period immutability** — *Why:* audit integrity. *Options:* snapshots only vs lock live settings.<br>6. **Payroll UI** — *Why:* batch/finalize UX. *Options:* new module vs tabs.<br>7. **Export deliverable** — *Why:* HM3 Phase 3. *Options:* print vs file export.<br>8. **ORM alternative** — *Why:* teammates may prefer JPA. *Design tension:* JDBC fits current CRC; ORM requires domain refactor—group should align on JDBC for this milestone (Planning v2 §Provisional Direction). |
