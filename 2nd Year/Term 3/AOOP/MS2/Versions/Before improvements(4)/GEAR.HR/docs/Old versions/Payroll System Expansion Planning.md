| SECTION                                  | GUIDING PROMPTS                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | ANSWER |
| ---------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| Reflections from Homework 1              | Briefly summarize key points from your Homework 1: Pre-Connection Planning that are relevant to persistence.<br><br>Consider:<br>What types of payroll and employee data your system manages<br>Key system tasks and user roles<br>Limitations caused by non-persistent or file-based data<br>Long-term goals you identified for the payroll system<br><br>Notes:<br>What parts of the system already seem tied to data handling?<br>What problems become harder without persistence?                                                                                                                                                         | GEAR.HR is a layered Java Swing desktop HR app. We manage six data areas: employees (~38 in seed data), attendance, leave, payroll settings (`PayrollData` per employee), login credentials, and IT tickets. Roles (`HR`, `Payroll`, `IT/Admin`, normal employee) control which modules appear; payroll is the most sensitive (salaries, rates, statutory deductions, net pay).<br><br>Today persistence is six CSV files under `csv/`, loaded/saved by repository classes; services orchestrate use cases; UI calls services via `ApplicationContext` only. Payroll combines stored settings with attendance hours and built-in Philippine deduction rules (SSS, PhilHealth, Pag-IBIG, withholding tax).<br><br>**Limitations tied to non-persistence:** single-user “last save wins,” no shared source of truth, weak audit (“who changed this pay?”), no durable company-wide pay history, employees can exist without payroll rows, reporting is mostly on-screen tables. Parts already tied to data handling: every service (`EmployeeService`, `AttendanceService`, `LeaveService`, `PayrollProcessor`, etc.) loads through `I*Repository`; payroll computation depends on attendance + payroll map.<br><br>**Long-term goals from HM1:** one organized MySQL store, same user-facing behavior, safer confidential data, foundation for history/reporting and (later) controlled multi-user access. |
| Vision for the Expanded Payroll System   | Describe how you expect your payroll system to evolve once persistence is introduced.<br><br>Focus on what the system should support over time, not how it will be built.<br><br>Address:<br>New capabilities enabled by persistence (e.g., historical records, reporting, automation)<br>How system usage may change as data accumulates<br>What reliable persistence means for your system in practice                                                                                                                                                                                                                                      | With database-backed persistence (per HM3), payroll evolves from per-employee calculator to **monthly office process**:<br><br>1. **Reports** — company-wide register and deduction summaries for a chosen month, aligned with employee pay slips.<br>2. **Audit** — log who changed pay settings or finalized a period (user, time, action type; field-level diffs deferred).<br>3. **Workflow** — named **pay periods** (draft → reviewed → finalized) with **frozen run snapshots** so history does not change when live rates change.<br><br>As data accumulates, payroll staff run batch computation for all employees, fix exceptions (missing hours/setup), finalize once, and employees/HR see consistent closed-month figures. Reliable persistence means one MySQL source of truth, coordinated saves (e.g. all snapshots + period status on finalize), and append-only audit evidence—not scattered files. |
| Current System at a Design Level         | Review your existing payroll system from a design perspective only.<br><br>You are not required to open or analyze code.<br><br>Describe:<br>The main components or classes in the system<br>How responsibilities are currently distributed<br>How payroll data flows during normal operation<br><br>If you did not build the system yourself, note any assumptions or uncertainties about its structure.                                                                                                                                                                                                                                     | **Layers and main types:**<br>- **ui:** `Main`, `User`, `EmployeeProfile`, `AttendanceScreen`, `LeaveManagementScreen`, `UserCredentialManagementScreen` — Swing screens; role checks in UI (e.g. HR view-only payroll).<br>- **service:** `ApplicationContext`, `*Service`, `PayrollProcessor`, `PayrollReport`, `RoleGroup` — use cases, validation orchestration, payroll computation.<br>- **repository:** `I*Repository`, `AbstractCsvListRepository`, concrete `*Repository` — load/save only; no business rules.<br>- **model:** `Employee`, `AttendanceRecord`, `LeaveRequest`, `PayrollData`, `PayrollResult`, `UserCredential`, `ItTicket` — domain state + `Validatable`.<br>- **util:** `PayrollUtils`, `*ValidationUtil` — statutory math and format validation.<br><br>**Payroll flow today:** `EmployeeProfile` → `PayrollProcessor.processPayroll(employee, month)` → load `PayrollData` from `IPayrollRepository` → `AttendanceService.getWorkedHoursForMonth` → return `PayrollResult` → optional `PayrollReport.format`. Pay settings CRUD goes through `PayrollProcessor` + repository; no pay period entity, no batch run, no audit log.<br><br>**Assumption:** This matches Architecture-Overview sections 6–8 and 11; we have not re-audited every UI branch in code for this homework. |
| Evaluating Persistence Design Options    | Using the Week 2 readings, reason about persistence design approaches at a structural level.<br><br>JDBC-Oriented Design (Conceptual)<br>Where would persistence-related responsibility likely live?<br>How visible would persistence behavior be in the system structure?<br>What design trade-offs might this introduce?<br><br>ORM-Oriented Design (Conceptual)<br>Where would persistence-related responsibility likely live?<br>How might abstraction change responsibility boundaries?<br>What design trade-offs might this introduce?<br><br>Do not decide implementation details.<br>Focus on responsibility placement and structure. | **JDBC-oriented (conceptual)**<br>- **Where responsibility lives:** `DatabaseConnectionManager` + `AbstractJdbcRepository` + concrete `*JdbcRepository` in `repository`; services still call `I*Repository`; `ApplicationContext` swaps implementations.<br>- **Visibility:** Persistence is explicit in structure (SQL, connection, repos); domain types stay unaware of storage.<br>- **Trade-offs:** More boilerplate and SQL to maintain; strong fit with existing repository interfaces and course JDBC lessons; clear boundaries; we control queries and transactions for payroll finalize.<br><br>**ORM-oriented (conceptual)**<br>- **Where responsibility lives:** Mapping layer (entities ↔ tables) often colocated with domain or a separate persistence package; services might call repositories or a higher-level data access facade.<br>- **Visibility:** SQL hidden; structure shows fewer repository classes, more annotations/mappers.<br>- **Trade-offs:** Faster CRUD for simple tables; learning curve and “magic” behavior; harder to model `PayrollRepository`’s `Map<String, PayrollData>` and HM3 snapshot/audit tables without careful mapping; less aligned with our current template-method CSV pattern.<br><br>We are not choosing ORM implementation details here—only noting that JDBC matches our existing **interface + swap** design. |
| Provisional Persistence Design Direction | Based on your reasoning above, identify your current, provisional design direction.<br><br>Select one:<br>☐ JDBC-oriented design<br>☐ ORM-oriented design<br><br>Then explain:<br>Why this direction fits your system’s goals at this stage<br>What structural consequences this choice suggests<br>What uncertainties or risks you still see<br><br>This is a design position, not a commitment.                                                                                                                                                                                                                                             | ☑ **JDBC-oriented design**<br>☐ ORM-oriented design<br><br>**Why:** HM3 Phase 1 and HM4 preserve `I*Repository` contracts and layered `ui → service → repository → model`; MySQL via JDBC is our course integration path. Minimal disruption: wire `EmployeeJdbcRepository`, etc., in `ApplicationContext` instead of CSV classes.<br><br>**Structural consequences:** New `DatabaseConnectionManager`, `AbstractJdbcRepository`, JDBC repos for six existing areas plus HM3 repos (`IPayPeriodRepository`, `IPayrollRunRepository`, `IAuditLogRepository`); extended `PayrollProcessor` / `PayrollReport`; CSV repos kept as legacy during migration.<br><br>**Risks/uncertainties:** Schema design and CSV→MySQL migration; transaction boundaries on batch finalize; whether finalized periods block live pay edits; performance of load-all-then-save patterns carried from CSV services. |
| Integration Goals (Outcomes-Focused)     | Clearly state what you want database-backed persistence to achieve.<br><br>Focus on outcomes, not technical details.<br><br>Examples:<br>improved accuracy and consistency of payroll records<br>support for historical payroll data<br>automation of payroll-related processes<br>enhanced reporting or auditing                                                                                                                                                                                                                                                                                                                             | - Single reliable MySQL store for employees, attendance, leave, payroll settings, credentials, and tickets.<br>- Improved accuracy and consistency (no conflicting file copies; relational links employee ↔ pay ↔ attendance).<br>- Support for **historical payroll** via pay periods and frozen snapshots.<br>- **Automation** of month-end: batch run all employees, exception list, finalize once.<br>- **Reporting and auditing** for payroll office and HR oversight (register, summaries, change log).<br>- Preserve role boundaries: payroll edits/finalize; HR view/review only; employees read own pay aligned to official runs. |
| Long-Term Design Implications            | Reflect briefly on how your current design direction supports long-term evolution.<br><br>Consider:<br>scalability as payroll data grows<br>maintainability as requirements change<br>flexibility to revise design decisions in later milestones                                                                                                                                                                                                                                                                                                                                                                                              | JDBC + repository interfaces support **scalability** for coursework scale (~38 employees, growing history tables) without rewriting UI. **Maintainability:** new storage = new repo class + wiring; services stay stable. **Flexibility:** HM3 phases 2–4 (periods, reports, audit) add domain/repos incrementally; we can revisit ORM, export format (PDF vs spreadsheet), or transaction granularity in later milestones without abandoning service contracts. |
| Open Questions and Uncertainties         | List:<br>assumptions you are least confident about<br>questions you want to revisit in Week 3<br>design tensions you expect to discuss with your group<br><br>These notes are especially important for later alignment.                                                                                                                                                                                                                                                                                                                                                                                                                       | - Exact MySQL schema (keys: keep string business keys vs surrogate IDs).<br>- One-time CSV import vs manual re-entry after schema creation.<br>- Export format for payroll register (PDF, CSV export, print-only).<br>- Period status labels and whether HR “reviewed” is a separate approval step or informational.<br>- Finalized period rules: block pay-setting edits for that month vs block re-run only.<br>- Transactions: only on `finalizePeriod` / `saveAllForPeriod`, or every save.<br>- Field-level audit vs action-type-only v1 (HM3 defers diffs). |
