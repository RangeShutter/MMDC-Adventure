# GEAR.HR database configuration.
# storage.mode: "jdbc" uses MySQL via JDBC repositories; "csv" falls back to legacy CSV repositories.
storage.mode=jdbc
db.url=jdbc:mysql://localhost:3306/gear.hr?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
db.user=root
db.password=Root@1234
